<Window x:Class="Watcher.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Watcher.Views"
        mc:Ignorable="d"
        Title="Configurações" Height="250" MinHeight="250" Width="300" MinWidth="300" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="5"></RowDefinition>
                <RowDefinition Height="0.3*"></RowDefinition>
                <RowDefinition Height="0.3*"></RowDefinition>
                <RowDefinition Height="0.3*"></RowDefinition>
                <RowDefinition Height="5"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="10"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="1" Grid.Column="1">
                <Label>Email</Label>
                <TextBox Height="30" x:Name="EmailInput" VerticalContentAlignment="Center"></TextBox>
            </StackPanel>

            <StackPanel Grid.Row="2" Grid.Column="1">
                <Label>N° Processo</Label>
                <TextBox Height="30" x:Name="NumberInput"  VerticalContentAlignment="Center"></TextBox>
            </StackPanel>

            <DockPanel Margin="0,5,0,0" Grid.Row="3" Grid.Column="1" VerticalAlignment="Center">
                <Label>Notificação por Email</Label>
                <CheckBox x:Name="NotifyCheckBox" VerticalAlignment="Center"></CheckBox>
                <Button x:Name="SaveButton" MinWidth="100" MinHeight="30" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Click="SaveButton_Click">Salvar</Button>
            </DockPanel>
        </Grid>
    </Grid>
</Window>
